#include <stdio.h>

int main()
{
    int number,i;
    int sum = 0;

    printf("Enter the number : \n");
    scanf("%d", &number);

    for (i=0;i<number;i++)
    {
        sum+=i;
        printf("%d ",sum);
    }

    return 0;
}